import './CardWithDescription.svelte.css.proxy.js';
/* src/components/CardWithDescription.svelte generated by Svelte v3.48.0 */
import {
	SvelteComponent,
	attr,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	transition_in,
	transition_out
} from "../../_snowpack/pkg/svelte/internal.js";

import Card from './Card.svelte.js';

function create_default_slot(ctx) {
	let div;

	return {
		c() {
			div = element("div");
			div.textContent = `${/*description*/ ctx[2]()}`;
			attr(div, "class", "description svelte-1lrnjkp");
		},
		m(target, anchor) {
			insert(target, div, anchor);
		},
		p: noop,
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function create_fragment(ctx) {
	let card_1;
	let current;

	card_1 = new Card({
			props: {
				card: /*card*/ ctx[0],
				isFirst: /*isFirst*/ ctx[1],
				$$slots: { default: [create_default_slot] },
				$$scope: { ctx }
			}
		});

	return {
		c() {
			create_component(card_1.$$.fragment);
		},
		m(target, anchor) {
			mount_component(card_1, target, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			const card_1_changes = {};
			if (dirty & /*card*/ 1) card_1_changes.card = /*card*/ ctx[0];
			if (dirty & /*isFirst*/ 2) card_1_changes.isFirst = /*isFirst*/ ctx[1];

			if (dirty & /*$$scope*/ 8) {
				card_1_changes.$$scope = { dirty, ctx };
			}

			card_1.$set(card_1_changes);
		},
		i(local) {
			if (current) return;
			transition_in(card_1.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(card_1.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(card_1, detaching);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { card } = $$props;
	let { isFirst } = $$props;

	function description() {
		const maxLength = 400;
		const str = card.description;

		if ((str === null || str === void 0 ? void 0 : str.length) && str.length <= maxLength) {
			return str;
		}

		return `${str.slice(0, maxLength - 1)}...`;
	}

	$$self.$$set = $$props => {
		if ('card' in $$props) $$invalidate(0, card = $$props.card);
		if ('isFirst' in $$props) $$invalidate(1, isFirst = $$props.isFirst);
	};

	return [card, isFirst, description];
}

class CardWithDescription extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { card: 0, isFirst: 1 });
	}
}

export default CardWithDescription;