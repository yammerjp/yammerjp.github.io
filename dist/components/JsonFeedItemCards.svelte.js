import './JsonFeedItemCards.svelte.css.proxy.js';
/* src/components/JsonFeedItemCards.svelte generated by Svelte v3.48.0 */
import {
	SvelteComponent,
	attr,
	check_outros,
	create_component,
	destroy_component,
	destroy_each,
	detach,
	element,
	group_outros,
	init,
	insert,
	mount_component,
	safe_not_equal,
	transition_in,
	transition_out
} from "../../_snowpack/pkg/svelte/internal.js";

import JsonFeedItemCard from './JsonFeedItemCard.svelte.js';

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[1] = list[i];
	child_ctx[3] = i;
	return child_ctx;
}

// (6:0) {#each items as item, i}
function create_each_block(ctx) {
	let jsonfeeditemcard;
	let current;

	jsonfeeditemcard = new JsonFeedItemCard({
			props: {
				item: /*item*/ ctx[1],
				isFirst: /*i*/ ctx[3] === 0
			}
		});

	return {
		c() {
			create_component(jsonfeeditemcard.$$.fragment);
		},
		m(target, anchor) {
			mount_component(jsonfeeditemcard, target, anchor);
			current = true;
		},
		p(ctx, dirty) {
			const jsonfeeditemcard_changes = {};
			if (dirty & /*items*/ 1) jsonfeeditemcard_changes.item = /*item*/ ctx[1];
			jsonfeeditemcard.$set(jsonfeeditemcard_changes);
		},
		i(local) {
			if (current) return;
			transition_in(jsonfeeditemcard.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(jsonfeeditemcard.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(jsonfeeditemcard, detaching);
		}
	};
}

function create_fragment(ctx) {
	let div;
	let current;
	let each_value = /*items*/ ctx[0];
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	const out = i => transition_out(each_blocks[i], 1, 1, () => {
		each_blocks[i] = null;
	});

	return {
		c() {
			div = element("div");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			attr(div, "class", "json-feed-item-cards svelte-1hc77u8");
		},
		m(target, anchor) {
			insert(target, div, anchor);

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(div, null);
			}

			current = true;
		},
		p(ctx, [dirty]) {
			if (dirty & /*items*/ 1) {
				each_value = /*items*/ ctx[0];
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
						transition_in(each_blocks[i], 1);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						transition_in(each_blocks[i], 1);
						each_blocks[i].m(div, null);
					}
				}

				group_outros();

				for (i = each_value.length; i < each_blocks.length; i += 1) {
					out(i);
				}

				check_outros();
			}
		},
		i(local) {
			if (current) return;

			for (let i = 0; i < each_value.length; i += 1) {
				transition_in(each_blocks[i]);
			}

			current = true;
		},
		o(local) {
			each_blocks = each_blocks.filter(Boolean);

			for (let i = 0; i < each_blocks.length; i += 1) {
				transition_out(each_blocks[i]);
			}

			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_each(each_blocks, detaching);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { items } = $$props;

	$$self.$$set = $$props => {
		if ('items' in $$props) $$invalidate(0, items = $$props.items);
	};

	return [items];
}

class JsonFeedItemCards extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { items: 0 });
	}
}

export default JsonFeedItemCards;