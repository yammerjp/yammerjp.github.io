/* src/components/Feeds.svelte generated by Svelte v3.48.0 */
import {
	SvelteComponent,
	create_component,
	destroy_component,
	detach,
	element,
	empty,
	handle_promise,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	transition_in,
	transition_out,
	update_await_block_branch
} from "../../_snowpack/pkg/svelte/internal.js";

import JsonFeedItemCards from './JsonFeedItemCards.svelte.js';
import { fetchFeeds } from '../feeds/feeds.js';

function create_catch_block(ctx) {
	let p;

	return {
		c() {
			p = element("p");
			p.textContent = "failed to fetch feeds...";
		},
		m(target, anchor) {
			insert(target, p, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(p);
		}
	};
}

// (3:0) {:then items}
function create_then_block(ctx) {
	let jsonfeeditemcards;
	let current;
	jsonfeeditemcards = new JsonFeedItemCards({ props: { items: /*items*/ ctx[0] } });

	return {
		c() {
			create_component(jsonfeeditemcards.$$.fragment);
		},
		m(target, anchor) {
			mount_component(jsonfeeditemcards, target, anchor);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(jsonfeeditemcards.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(jsonfeeditemcards.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(jsonfeeditemcards, detaching);
		}
	};
}

// (1:58)    <p>...waiting</p> {:then items}
function create_pending_block(ctx) {
	let p;

	return {
		c() {
			p = element("p");
			p.textContent = "...waiting";
		},
		m(target, anchor) {
			insert(target, p, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(p);
		}
	};
}

function create_fragment(ctx) {
	let await_block_anchor;
	let promise;
	let current;

	let info = {
		ctx,
		current: null,
		token: null,
		hasCatch: true,
		pending: create_pending_block,
		then: create_then_block,
		catch: create_catch_block,
		value: 0,
		blocks: [,,,]
	};

	handle_promise(promise = fetchFeeds('https://rsss.yammer.jp/v0/json_feed'), info);

	return {
		c() {
			await_block_anchor = empty();
			info.block.c();
		},
		m(target, anchor) {
			insert(target, await_block_anchor, anchor);
			info.block.m(target, info.anchor = anchor);
			info.mount = () => await_block_anchor.parentNode;
			info.anchor = await_block_anchor;
			current = true;
		},
		p(new_ctx, [dirty]) {
			ctx = new_ctx;
			update_await_block_branch(info, ctx, dirty);
		},
		i(local) {
			if (current) return;
			transition_in(info.block);
			current = true;
		},
		o(local) {
			for (let i = 0; i < 3; i += 1) {
				const block = info.blocks[i];
				transition_out(block);
			}

			current = false;
		},
		d(detaching) {
			if (detaching) detach(await_block_anchor);
			info.block.d(detaching);
			info.token = null;
			info = null;
		}
	};
}

class Feeds extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Feeds;