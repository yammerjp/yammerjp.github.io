import './Card.svelte.css.proxy.js';
/* src/components/Card.svelte generated by Svelte v3.48.0 */
import {
	SvelteComponent,
	append,
	attr,
	create_slot,
	detach,
	element,
	get_all_dirty_from_scope,
	get_slot_changes,
	init,
	insert,
	safe_not_equal,
	set_data,
	space,
	text,
	transition_in,
	transition_out,
	update_slot_base
} from "../../_snowpack/pkg/svelte/internal.js";

function create_if_block(ctx) {
	let h3;
	let t_value = /*card*/ ctx[0].title + "";
	let t;

	return {
		c() {
			h3 = element("h3");
			t = text(t_value);
			attr(h3, "class", "title svelte-2iy56v");
		},
		m(target, anchor) {
			insert(target, h3, anchor);
			append(h3, t);
		},
		p(ctx, dirty) {
			if (dirty & /*card*/ 1 && t_value !== (t_value = /*card*/ ctx[0].title + "")) set_data(t, t_value);
		},
		d(detaching) {
			if (detaching) detach(h3);
		}
	};
}

function create_fragment(ctx) {
	let a;
	let div2;
	let t0;
	let div0;
	let t2;
	let div1;
	let a_href_value;
	let a_class_value;
	let current;
	let if_block = /*card*/ ctx[0].title && create_if_block(ctx);
	const default_slot_template = /*#slots*/ ctx[4].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[3], null);

	return {
		c() {
			a = element("a");
			div2 = element("div");
			if (if_block) if_block.c();
			t0 = space();
			div0 = element("div");
			div0.textContent = `${/*dateAndSite*/ ctx[2]()}`;
			t2 = space();
			div1 = element("div");
			if (default_slot) default_slot.c();
			attr(div0, "class", "date_and_site svelte-2iy56v");
			attr(div1, "class", "content svelte-2iy56v");
			attr(div2, "class", "article svelte-2iy56v");
			attr(a, "href", a_href_value = /*card*/ ctx[0].url);
			attr(a, "class", a_class_value = "article-wrapper-link " + (/*isFirst*/ ctx[1] ? 'is-first' : '') + " svelte-2iy56v");
		},
		m(target, anchor) {
			insert(target, a, anchor);
			append(a, div2);
			if (if_block) if_block.m(div2, null);
			append(div2, t0);
			append(div2, div0);
			append(div2, t2);
			append(div2, div1);

			if (default_slot) {
				default_slot.m(div1, null);
			}

			current = true;
		},
		p(ctx, [dirty]) {
			if (/*card*/ ctx[0].title) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					if_block.m(div2, t0);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}

			if (default_slot) {
				if (default_slot.p && (!current || dirty & /*$$scope*/ 8)) {
					update_slot_base(
						default_slot,
						default_slot_template,
						ctx,
						/*$$scope*/ ctx[3],
						!current
						? get_all_dirty_from_scope(/*$$scope*/ ctx[3])
						: get_slot_changes(default_slot_template, /*$$scope*/ ctx[3], dirty, null),
						null
					);
				}
			}

			if (!current || dirty & /*card*/ 1 && a_href_value !== (a_href_value = /*card*/ ctx[0].url)) {
				attr(a, "href", a_href_value);
			}

			if (!current || dirty & /*isFirst*/ 2 && a_class_value !== (a_class_value = "article-wrapper-link " + (/*isFirst*/ ctx[1] ? 'is-first' : '') + " svelte-2iy56v")) {
				attr(a, "class", a_class_value);
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);
			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(a);
			if (if_block) if_block.d();
			if (default_slot) default_slot.d(detaching);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	let { card } = $$props;
	let { isFirst } = $$props;

	function date() {
		const d = new Date(card.isoDate);
		const yyyy = `${d.getFullYear()}`;
		const mm = `00${d.getMonth() + 1}`.slice(-2);
		const dd = `00${d.getDate()}`.slice(-2);
		return `${yyyy}/${mm}/${dd}`;
	}

	function dateAndSite() {
		if (card.siteName) {
			return `${date()} - ${card.siteName}`;
		}

		return date();
	}

	$$self.$$set = $$props => {
		if ('card' in $$props) $$invalidate(0, card = $$props.card);
		if ('isFirst' in $$props) $$invalidate(1, isFirst = $$props.isFirst);
		if ('$$scope' in $$props) $$invalidate(3, $$scope = $$props.$$scope);
	};

	return [card, isFirst, dateAndSite, $$scope, slots];
}

class Card extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { card: 0, isFirst: 1 });
	}
}

export default Card;